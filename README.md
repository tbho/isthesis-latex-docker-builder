# isthesis-latex-docker-builder

Run the following command to generate the pdf.


zsh-shell:
`docker run --rm -v $(pwd)/:/tex/ tbho/isthesis-latex-docker-builder`

fish-shell:
`docker run --rm -v ('pwd'):/tex/ tbho/isthesis-latex-docker-builder`
